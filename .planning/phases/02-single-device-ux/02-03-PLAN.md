---
phase: 02-single-device-ux
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - AgingInPlace/Features/CareVisit/LogCareVisitView.swift
  - AgingInPlace/Features/Mood/LogMoodView.swift
  - AgingInPlace/Features/Mood/MoodPickerView.swift
  - AgingInPlaceTests/CareVisitTests.swift
  - AgingInPlaceTests/MoodTests.swift
autonomous: true
requirements:
  - CARE-01
  - CARE-02
  - CARE-03

must_haves:
  truths:
    - "Caregiver can log a care visit with meals, mobility, observations, and concerns"
    - "Senior can self-report mood on a 1-5 scale"
    - "Caregiver can log observed mood with distinct authorship from senior self-report"
    - "Care visit data is encrypted before storage"
    - "Mood entries show correct author type (senior vs caregiver)"
  artifacts:
    - path: "AgingInPlace/Features/CareVisit/LogCareVisitView.swift"
      provides: "Care visit logging form with structured fields"
      contains: "struct LogCareVisitView"
    - path: "AgingInPlace/Features/Mood/LogMoodView.swift"
      provides: "Mood logging form for both senior and caregiver"
      contains: "struct LogMoodView"
    - path: "AgingInPlace/Features/Mood/MoodPickerView.swift"
      provides: "5-option mood selector with accessibility labels"
      contains: "struct MoodPickerView"
    - path: "AgingInPlaceTests/CareVisitTests.swift"
      provides: "Unit tests for care visit logging"
    - path: "AgingInPlaceTests/MoodTests.swift"
      provides: "Unit tests for mood logging"
  key_links:
    - from: "LogCareVisitView"
      to: "EncryptionService.seal"
      via: "Encrypts care visit payload before SwiftData insert"
      pattern: "EncryptionService\\.seal.*\\.careVisits"
    - from: "LogMoodView"
      to: "MoodLog"
      via: "Creates MoodLog with authorType distinguishing senior vs caregiver"
      pattern: "MoodLog.*authorType"
---

<objective>
Build care visit logging and mood logging features with encrypted storage and distinct authorship tracking.

Purpose: Care documentation and mood tracking are core workflows. Caregivers log structured visit notes; seniors and caregivers both log mood with clear attribution.
Output: Care visit form, mood form with picker, unit tests for both
</objective>

<execution_context>
@/Users/dustinober/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dustinober/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-single-device-ux/02-RESEARCH.md
@.planning/phases/02-single-device-ux/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 -->

From AgingInPlace/Models/CareVisitLog.swift:
```swift
@Model final class CareVisitLog {
    var id: UUID
    var encryptedPayload: Data  // AES-GCM: { meals, mobility, observations, concerns }
    var visitDate: Date
    var authorMemberID: UUID
    var createdAt: Date
    var lastModified: Date
}
```

From AgingInPlace/Models/MoodLog.swift:
```swift
@Model final class MoodLog {
    var id: UUID
    var moodValue: Int  // 1-5
    var authorMemberID: UUID
    var authorType: MoodAuthorType  // .senior or .caregiver
    var notes: Data?  // Optional encrypted
    var loggedAt: Date
    var lastModified: Date
}

enum MoodAuthorType: String, Codable, CaseIterable {
    case senior
    case caregiver
}
```

From AgingInPlace/Encryption/EncryptionService.swift:
```swift
struct EncryptionService {
    static func seal(_ plaintext: Data, for category: PermissionCategory) throws -> Data
    static func open(_ ciphertext: Data, for category: PermissionCategory) throws -> Data
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Care visit logging form with encrypted storage and tests</name>
  <files>
    AgingInPlace/Features/CareVisit/LogCareVisitView.swift,
    AgingInPlaceTests/CareVisitTests.swift
  </files>
  <behavior>
    - Test: CareVisitLog with encrypted payload containing meals, mobility, observations, concerns round-trips through encrypt/decrypt
    - Test: CareVisitLog visitDate is stored as plaintext Date (not encrypted) for predicate filtering
    - Test: CareVisitLog authorMemberID matches the creating user
  </behavior>
  <action>
    1. Write CareVisitTests.swift FIRST (TDD red phase):
       - Test encrypted payload round-trip: create JSON with {meals, mobility, observations, concerns}, seal with .careVisits, create CareVisitLog, save, fetch, open, verify fields match
       - Test visitDate stored correctly for predicate use
       - Test authorMemberID persists correctly

    2. Create AgingInPlace/Features/CareVisit/ directory.

    3. Create LogCareVisitView.swift — structured care visit form:
       - Form with sections:
         - "Visit Details": visitDate (DatePicker, defaults to now)
         - "Meals": meals (TextEditor, 3 lines minimum, placeholder "What did they eat?")
         - "Mobility": mobility (TextEditor, placeholder "How was their movement today?")
         - "Observations": observations (TextEditor, placeholder "General observations...")
         - "Concerns": concerns (TextEditor, placeholder "Any concerns to note?")
       - Save button in toolbar. On save:
         - JSON-encode {meals, mobility, observations, concerns} as Data
         - Call EncryptionService.seal(jsonData, for: .careVisits)
         - Create CareVisitLog(encryptedPayload:, visitDate:, authorMemberID:) where authorMemberID comes from @AppStorage("caregiverMemberID") or current user ID
         - context.insert + context.save()
         - Dismiss
       - Validate at least one field is non-empty before enabling Save.
       - All touch targets 44pt minimum. TextEditors have .frame(minHeight: 80).
  </action>
  <verify>
    <automated>xcodebuild test -scheme AgingInPlace -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:AgingInPlaceTests/CareVisitTests 2>&1 | tail -20</automated>
  </verify>
  <done>LogCareVisitView saves encrypted care visit logs. Encrypted payload round-trips correctly. All care visit tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mood logging with 5-option picker, author type, and tests</name>
  <files>
    AgingInPlace/Features/Mood/LogMoodView.swift,
    AgingInPlace/Features/Mood/MoodPickerView.swift,
    AgingInPlaceTests/MoodTests.swift
  </files>
  <behavior>
    - Test: MoodLog with authorType .senior saves moodValue correctly
    - Test: MoodLog with authorType .caregiver saves moodValue correctly
    - Test: MoodLog with optional encrypted notes round-trips
    - Test: MoodLog without notes saves notes as nil
    - Test: Fetch MoodLogs sorted by loggedAt returns correct order
  </behavior>
  <action>
    1. Write MoodTests.swift FIRST (TDD red phase):
       - Test senior mood log: create MoodLog(moodValue: 4, authorType: .senior), save, fetch, verify moodValue and authorType
       - Test caregiver mood log: create MoodLog(moodValue: 2, authorType: .caregiver), save, fetch, verify distinct from senior
       - Test notes encryption: seal notes with .mood category, save, fetch, open, verify text
       - Test nil notes: save MoodLog without notes, fetch, verify notes is nil
       - Test sort order: insert two logs with different loggedAt, fetch sorted descending, verify order

    2. Create AgingInPlace/Features/Mood/ directory.

    3. Create MoodPickerView.swift — a reusable 5-option horizontal picker:
       - @Binding var selectedMood: Int (1-5)
       - HStack with 5 buttons, each showing an emoji: 1="very_sad" 2="sad" 3="neutral" 4="happy" 5="very_happy"
       - Use emoji characters directly in Text: "sad" = "feeling_sad", "happy" = "feeling_happy" etc. OR use SF Symbols if preferred. Follow EXACTLY the 02-RESEARCH.md Pattern 5 with emoji: (1, "very_sad_face", "Very sad"), (2, "sad_face", "Sad"), (3, "neutral_face", "Neutral"), (4, "happy_face", "Happy"), (5, "very_happy_face", "Very happy").
       - Selected mood has accentColor background circle. All buttons 44pt minimum.
       - Each button has .accessibilityLabel with the mood name and .accessibilityAddTraits(.isSelected) when active.

    4. Create LogMoodView.swift — mood entry form:
       - Takes authorType: MoodAuthorType as init parameter (determines if this is senior self-report or caregiver observed)
       - Shows title: authorType == .senior ? "How are you feeling?" : "How is [seniorName] feeling?"
       - MoodPickerView bound to @State var selectedMood: Int = 3
       - Optional notes TextEditor ("Add a note..." placeholder)
       - Save button: Create MoodLog with moodValue, authorMemberID, authorType, optional encrypted notes (if non-empty, seal with .mood). context.insert + context.save(). Dismiss.
       - Validate selectedMood is 1-5 (always true with picker).
  </action>
  <verify>
    <automated>xcodebuild test -scheme AgingInPlace -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:AgingInPlaceTests/MoodTests 2>&1 | tail -20</automated>
  </verify>
  <done>MoodPickerView renders 5 options with accessibility labels. LogMoodView saves MoodLog with correct authorType. Senior and caregiver mood entries are distinct. All mood tests pass.</done>
</task>

</tasks>

<verification>
```bash
xcodebuild test -scheme AgingInPlace -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:AgingInPlaceTests/CareVisitTests -only-testing:AgingInPlaceTests/MoodTests 2>&1 | tail -20
```
</verification>

<success_criteria>
- Caregiver can log a care visit with structured fields (meals, mobility, observations, concerns) encrypted before storage (CARE-01)
- Senior can self-report mood on a 1-5 scale with emoji picker (CARE-02)
- Caregiver can log observed mood with distinct .caregiver authorType (CARE-03)
- All care visit and mood tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-single-device-ux/02-03-SUMMARY.md`
</output>
