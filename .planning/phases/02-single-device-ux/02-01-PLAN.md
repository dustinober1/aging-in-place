---
phase: 02-single-device-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AgingInPlace/Models/Schema/AgingInPlaceSchemaV1.swift
  - AgingInPlace/Models/Schema/AgingInPlaceSchemaV2.swift
  - AgingInPlace/Models/Schema/AgingInPlaceMigrationPlan.swift
  - AgingInPlace/Models/MedicationSchedule.swift
  - AgingInPlace/Models/MedicationLog.swift
  - AgingInPlace/Models/CareVisitLog.swift
  - AgingInPlace/Models/MoodLog.swift
  - AgingInPlace/Models/CalendarEvent.swift
  - AgingInPlace/Notifications/NotificationService.swift
  - AgingInPlace/App/AgingInPlaceApp.swift
  - AgingInPlaceTests/SchemaMigrationTests.swift
  - AgingInPlaceTests/NotificationServiceTests.swift
  - AgingInPlace/AgingInPlace.entitlements
autonomous: true
requirements:
  - MEDS-01
  - MEDS-02
  - MEDS-05
  - CARE-01
  - CARE-02
  - CARE-03
  - CALR-01
  - CALR-03

must_haves:
  truths:
    - "App launches without crash after schema migration from V1 to V2"
    - "All five new model types can be inserted, saved, and fetched from SwiftData"
    - "NotificationService can schedule and cancel medication reminders, missed-dose alerts, and appointment reminders"
    - "Encryption service can seal and open payloads for new model types using existing PermissionCategory keys"
  artifacts:
    - path: "AgingInPlace/Models/Schema/AgingInPlaceSchemaV1.swift"
      provides: "V1 schema wrapping Phase 1 models"
      contains: "AgingInPlaceSchemaV1"
    - path: "AgingInPlace/Models/Schema/AgingInPlaceSchemaV2.swift"
      provides: "V2 schema adding Phase 2 models"
      contains: "AgingInPlaceSchemaV2"
    - path: "AgingInPlace/Models/Schema/AgingInPlaceMigrationPlan.swift"
      provides: "Lightweight migration from V1 to V2"
      contains: "AgingInPlaceMigrationPlan"
    - path: "AgingInPlace/Models/MedicationSchedule.swift"
      provides: "Recurring medication schedule model"
      contains: "class MedicationSchedule"
    - path: "AgingInPlace/Models/MedicationLog.swift"
      provides: "Medication administration log model"
      contains: "class MedicationLog"
    - path: "AgingInPlace/Models/CareVisitLog.swift"
      provides: "Care visit log model"
      contains: "class CareVisitLog"
    - path: "AgingInPlace/Models/MoodLog.swift"
      provides: "Mood log model with author type"
      contains: "class MoodLog"
    - path: "AgingInPlace/Models/CalendarEvent.swift"
      provides: "Calendar event model"
      contains: "class CalendarEvent"
    - path: "AgingInPlace/Notifications/NotificationService.swift"
      provides: "Notification scheduling and cancellation"
      contains: "NotificationService"
    - path: "AgingInPlace/AgingInPlace.entitlements"
      provides: "Time-sensitive notification entitlement"
      contains: "com.apple.developer.usernotifications.time-sensitive"
  key_links:
    - from: "AgingInPlace/App/AgingInPlaceApp.swift"
      to: "AgingInPlaceSchemaV2.models"
      via: "modelContainer with migrationPlan"
      pattern: "migrationPlan.*AgingInPlaceMigrationPlan"
    - from: "AgingInPlaceSchemaV2"
      to: "All 10 model types"
      via: "models array"
      pattern: "MedicationSchedule.self.*MedicationLog.self.*CareVisitLog.self.*MoodLog.self.*CalendarEvent.self"
---

<objective>
Create VersionedSchema migration infrastructure, all five Phase 2 SwiftData models, NotificationService, and the time-sensitive entitlement.

Purpose: This is the foundation for all Phase 2 feature work. No UI can be built until models exist and the schema migration is proven safe. NotificationService is shared across medications and calendar features.
Output: 8 new model/schema files, NotificationService, entitlements, migration tests, notification tests
</objective>

<execution_context>
@/Users/dustinober/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dustinober/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-single-device-ux/02-RESEARCH.md

<interfaces>
<!-- Existing Phase 1 interfaces this plan depends on -->

From AgingInPlace/Models/PermissionCategory.swift:
```swift
enum PermissionCategory: String, Codable, CaseIterable {
    case medications
    case mood
    case careVisits
    case calendar
}
```

From AgingInPlace/Encryption/EncryptionService.swift:
```swift
struct EncryptionService {
    static func seal(_ plaintext: Data, for category: PermissionCategory) throws -> Data
    static func open(_ ciphertext: Data, for category: PermissionCategory) throws -> Data
    static func rotateKey(for category: PermissionCategory) throws
}
```

From AgingInPlace/App/AgingInPlaceApp.swift:
```swift
@main
struct AgingInPlaceApp: App {
    var body: some Scene {
        WindowGroup { RootView() }
        .modelContainer(
            for: [CareCircle.self, CareTeamMember.self, CareRecord.self, InviteCode.self, EmergencyContact.self],
            isAutosaveEnabled: false
        )
    }
}
```

Phase 1 models (to be wrapped in SchemaV1):
- CareCircle, CareTeamMember, CareRecord, InviteCode, EmergencyContact
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: VersionedSchema migration, five new models, entitlements, and migration tests</name>
  <files>
    AgingInPlace/Models/Schema/AgingInPlaceSchemaV1.swift,
    AgingInPlace/Models/Schema/AgingInPlaceSchemaV2.swift,
    AgingInPlace/Models/Schema/AgingInPlaceMigrationPlan.swift,
    AgingInPlace/Models/MedicationSchedule.swift,
    AgingInPlace/Models/MedicationLog.swift,
    AgingInPlace/Models/CareVisitLog.swift,
    AgingInPlace/Models/MoodLog.swift,
    AgingInPlace/Models/CalendarEvent.swift,
    AgingInPlace/App/AgingInPlaceApp.swift,
    AgingInPlace/AgingInPlace.entitlements,
    AgingInPlaceTests/SchemaMigrationTests.swift
  </files>
  <behavior>
    - Test: In-memory ModelContainer with AgingInPlaceSchemaV2.models and AgingInPlaceMigrationPlan opens without crash
    - Test: MedicationSchedule can be inserted and fetched with all fields intact
    - Test: MedicationLog with encrypted payload round-trips through insert/save/fetch
    - Test: CareVisitLog with encrypted payload round-trips through insert/save/fetch
    - Test: MoodLog with moodValue 1-5 and both authorType cases (.senior, .caregiver) saves correctly
    - Test: CalendarEvent with title, eventDate, and encrypted payload round-trips
    - Test: All Phase 1 models (CareCircle, CareTeamMember, CareRecord, InviteCode, EmergencyContact) still insertable after V2 migration
  </behavior>
  <action>
    1. Create AgingInPlace/Models/Schema/ directory.

    2. Create AgingInPlaceSchemaV1.swift: enum conforming to VersionedSchema with versionIdentifier Schema.Version(1, 0, 0). Its models array lists exactly the 5 Phase 1 types: CareCircle.self, CareTeamMember.self, CareRecord.self, InviteCode.self, EmergencyContact.self.

    3. Create all 5 new @Model classes following EXACTLY the research patterns in 02-RESEARCH.md Pattern 2. Key points:
       - MedicationSchedule: drugName (String, not encrypted — used in notifications), dose (String, not encrypted), scheduledHour/scheduledMinute (Int), missedWindowMinutes (Int, default 30), isActive (Bool), createdByMemberID (UUID), createdAt/lastModified (Date). No encryptedPayload — drug name and dose are needed for notification content and predicate filtering.
       - MedicationLog: scheduleID (UUID?, optional link), encryptedPayload (Data — AES-GCM sealed JSON of drugName, dose, notes), administeredAt (Date — NOT encrypted, needed for predicate sorting), authorMemberID (UUID), createdAt/lastModified.
       - CareVisitLog: encryptedPayload (Data — AES-GCM sealed JSON of meals, mobility, observations, concerns), visitDate (Date — NOT encrypted), authorMemberID (UUID), createdAt/lastModified.
       - MoodLog: moodValue (Int, 1-5, NOT encrypted — not PHI alone), authorMemberID (UUID), authorType (MoodAuthorType enum: .senior/.caregiver as String raw value Codable), notes (Data? — optional encrypted), loggedAt (Date), lastModified. Define MoodAuthorType enum in same file.
       - CalendarEvent: title (String, NOT encrypted — needed in notifications), eventDate (Date, NOT encrypted), reminderOffsetMinutes (Int, default 60), encryptedPayload (Data — location, notes, attendees), createdByMemberID (UUID), createdAt/lastModified.
       - ALL models have var id: UUID initialized in init.
       - ALL models have lastModified: Date for Phase 3 LWW sync.

    4. Create AgingInPlaceSchemaV2.swift: enum conforming to VersionedSchema with versionIdentifier Schema.Version(2, 0, 0). Its models array lists ALL 10 types (5 Phase 1 + 5 Phase 2).

    5. Create AgingInPlaceMigrationPlan.swift: enum conforming to SchemaMigrationPlan. schemas = [AgingInPlaceSchemaV1.self, AgingInPlaceSchemaV2.self]. stages = [migrateV1toV2] where migrateV1toV2 = MigrationStage.lightweight(fromVersion: AgingInPlaceSchemaV1.self, toVersion: AgingInPlaceSchemaV2.self).

    6. Update AgingInPlaceApp.swift: Change .modelContainer to use `for: AgingInPlaceSchemaV2.models, migrationPlan: AgingInPlaceMigrationPlan.self, isAutosaveEnabled: false`.

    7. Create AgingInPlace.entitlements plist with com.apple.developer.usernotifications.time-sensitive = true. Update project.yml or Xcode project to reference the entitlements file.

    8. Write SchemaMigrationTests.swift with the behavior tests above. Use in-memory ModelConfiguration. Each test creates a ModelContainer with the migration plan, inserts a model instance, calls context.save(), fetches, and asserts fields match.
  </action>
  <verify>
    <automated>xcodebuild test -scheme AgingInPlace -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:AgingInPlaceTests/SchemaMigrationTests 2>&1 | tail -20</automated>
  </verify>
  <done>All 7+ schema migration tests pass. App builds without warnings. All 10 model types are insertable in the V2 schema. Entitlements file exists with time-sensitive key.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NotificationService with deterministic identifiers and unit tests</name>
  <files>
    AgingInPlace/Notifications/NotificationService.swift,
    AgingInPlaceTests/NotificationServiceTests.swift
  </files>
  <behavior>
    - Test: scheduleMedicationReminder creates a request with identifier "med-reminder-{uuid}" and UNCalendarNotificationTrigger with repeats:true
    - Test: scheduleMissedDoseAlert creates a request with identifier "med-missed-{uuid}-{dateKey}" and UNTimeIntervalNotificationTrigger with repeats:false
    - Test: cancelMissedDoseAlert removes the correct identifier
    - Test: scheduleAppointmentReminder creates a request with identifier "cal-reminder-{uuid}" and correct trigger interval
    - Test: cancelAppointmentReminder removes the correct identifier
    - Test: Notification content uses generic titles ("Medication Reminder", "Missed Dose Alert", "Upcoming Appointment") — no PHI in title or body (drug name is acceptable per research — it is not PHI in isolation)
    - Test: Medication reminder uses .timeSensitive interruption level
  </behavior>
  <action>
    1. Create AgingInPlace/Notifications/ directory.

    2. Implement NotificationService as a struct with static methods, following EXACTLY the 02-RESEARCH.md Pattern 3:
       - requestAuthorization() async throws -> Bool
       - scheduleMedicationReminder(for: MedicationSchedule) async throws — identifier: "med-reminder-\(schedule.id.uuidString)", UNCalendarNotificationTrigger with hour/minute, repeats: true, interruptionLevel: .timeSensitive. Content title: "Medication Reminder", body: "\(schedule.drugName) — \(schedule.dose)".
       - scheduleMissedDoseAlert(for: MedicationSchedule, onDate: Date) async throws — identifier: "med-missed-\(schedule.id.uuidString)-\(dateKey)" where dateKey is ISO8601 date prefix (YYYY-MM-DD). UNTimeIntervalNotificationTrigger, repeats: false. Content title: "Missed Dose Alert", body: "\(schedule.drugName) was not confirmed within \(schedule.missedWindowMinutes) minutes". interruptionLevel: .timeSensitive.
       - cancelMissedDoseAlert(for: MedicationSchedule, onDate: Date) — removePendingNotificationRequests with deterministic identifier.
       - scheduleAppointmentReminder(for: CalendarEvent) async throws — identifier: "cal-reminder-\(event.id.uuidString)". UNTimeIntervalNotificationTrigger. Content title: "Upcoming Appointment", body: "\(event.title) starts in \(event.reminderOffsetMinutes) minutes". interruptionLevel: .active. Guard against past events.
       - cancelAppointmentReminder(for: CalendarEvent) — removePendingNotificationRequests.
       - refreshScheduledNotifications(schedules: [MedicationSchedule]) async throws — removes existing med-reminder-* notifications and re-schedules active ones.

    3. Write NotificationServiceTests.swift. Since UNUserNotificationCenter is a singleton, tests verify:
       - After scheduling, getPendingNotificationRequests() contains the expected identifier
       - After cancelling, the identifier is no longer pending
       - Content fields match expected values (generic titles, correct body)
       - Trigger types are correct (calendar vs time-interval, repeats flag)

    Note: UNUserNotificationCenter works in the test host on Simulator. Use `await UNUserNotificationCenter.current().pendingNotificationRequests()` to verify scheduled notifications.
  </action>
  <verify>
    <automated>xcodebuild test -scheme AgingInPlace -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:AgingInPlaceTests/NotificationServiceTests 2>&1 | tail -20</automated>
  </verify>
  <done>All notification tests pass. NotificationService correctly schedules and cancels notifications with deterministic identifiers. No PHI leaks in notification content beyond drug name (acceptable per research). Medication reminders use timeSensitive interruption level.</done>
</task>

</tasks>

<verification>
Run full test suite to confirm no regressions from Phase 1:
```bash
xcodebuild test -scheme AgingInPlace -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -30
```
All Phase 1 tests (encryption, persistence, LWW, invite, care team, permissions, emergency contacts, senior home, caregiver home) plus new schema migration and notification tests must pass.
</verification>

<success_criteria>
- App builds and launches without crash on iOS Simulator
- VersionedSchema migration from V1 to V2 succeeds (no "unknown model version" crash)
- All 5 new model types insertable in SwiftData with encrypted payloads
- NotificationService schedules/cancels with deterministic identifiers
- Time-sensitive entitlement present
- All Phase 1 + Phase 2 tests pass (0 failures)
</success_criteria>

<output>
After completion, create `.planning/phases/02-single-device-ux/02-01-SUMMARY.md`
</output>
