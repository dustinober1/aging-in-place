---
phase: 01-foundation
plan: 05
type: execute
wave: 3
depends_on: ["01-02", "01-03", "01-04"]
files_modified:
  - AgingInPlace/Features/CareTeam/CareTeamListView.swift
autonomous: false
requirements:
  - SYNC-01
  - SYNC-03
  - SYNC-04
  - SYNC-05
  - SYNC-08
  - SENR-01
  - SENR-02
  - SENR-03
  - SENR-04
  - TEAM-01
  - TEAM-02
  - TEAM-03
  - TEAM-04
  - TEAM-05
  - TEAM-06
  - TEAM-07
  - TEAM-08
  - TEAM-09

must_haves:
  truths:
    - "Full test suite passes with zero failures"
    - "Senior can invite a caregiver, caregiver can join, both see each other on the team list"
    - "Permission revocation triggers key rotation and new records are protected"
    - "Senior home screen is usable at Dynamic Type XXL+ with 44pt touch targets"
    - "Data persists across simulated app relaunch"
    - "CareTeamListView navigates to MemberDetailView (not placeholder)"
---

<objective>
Run the full test suite and verify the complete Phase 1 experience end-to-end in the simulator — confirming that invite/join, permissions, encryption, home screens, and persistence all work together.

Purpose: Integration verification before marking Phase 1 complete. Catches wiring issues between plans that unit tests may miss.
Output: Confirmed working Phase 1 with all tests green and visual verification.
</objective>

<execution_context>
@/Users/dustinober/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dustinober/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
@.planning/phases/01-foundation/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire placeholder NavigationLinks, full test suite, and build verification</name>
  <files>
    AgingInPlace/Features/CareTeam/CareTeamListView.swift
  </files>
  <action>
    **Step 1 — Wire MemberDetailView into CareTeamListView:**
    Plan 02 created CareTeamListView with a placeholder NavigationLink destination (`Text("Member Detail")`). Plan 03 created MemberDetailView. Now that both are complete, update CareTeamListView to replace the placeholder with `MemberDetailView(member: member)`. This is a single-line change in CareTeamListView.swift.

    **Step 2 — Run the complete test suite and fix integration issues:**
    1. Run full build: `xcodebuild build -scheme AgingInPlace -destination 'platform=iOS Simulator,name=iPhone 16'`
    2. Run full test suite: `xcodebuild test -scheme AgingInPlace -destination 'platform=iOS Simulator,name=iPhone 16'`
    3. Verify all test files pass:
       - EncryptionTests (seal/open/rotate)
       - PersistenceTests (insert/save/fetch)
       - LWWTests (timestamp comparison)
       - InviteCodeTests (format validation)
       - InviteFlowTests (create/accept/reject)
       - CareTeamTests (add/remove members)
       - PermissionTests (grant/revoke/rotation)
       - EmergencyContactTests (CRUD without permissions)
       - SeniorHomeTests (greeting/cards)
       - CaregiverHomeTests (filtered activity)

    4. If any tests fail due to integration issues (e.g., missing imports, type mismatches between plans), fix the issues and re-run.

    5. Verify no Swift 6 strict concurrency warnings remain.
  </action>
  <verify>
    <automated>xcodebuild test -scheme AgingInPlace -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | grep -E "(Test Suite|Executed|FAILED|PASSED)" | tail -20</automated>
  </verify>
  <done>CareTeamListView navigates to MemberDetailView (placeholder removed). All test suites pass with zero failures. Build succeeds with zero errors and zero warnings.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional verification of Phase 1</name>
  <action>
    Present the completed Phase 1 app to the user for visual and functional verification in the iOS Simulator. The user will verify accessibility, invite flow, permissions, emergency contacts, and data persistence.
  </action>
  <what-built>Complete Phase 1 foundation: care team invite/join flow, permission management with key rotation, senior home screen with accessibility, caregiver home with activity feed, emergency contacts</what-built>
  <how-to-verify>
    1. Open AgingInPlace.xcodeproj in Xcode
    2. Run on iPhone 16 simulator (iOS 17+)
    3. Select "I am the Senior" role

    **Senior Home Screen (SENR-01, SENR-02, SENR-03, SENR-04):**
    4. Verify greeting shows correct time-of-day ("Good morning/afternoon/evening")
    5. Verify 4 large cards: Medications, Mood, Care Team, Calendar
    6. Verify cards are large, tappable, and use system colors
    7. Go to Settings > Accessibility > Display > Larger Text > enable "Larger Accessibility Sizes" > set to maximum
    8. Return to app — verify layout adapts (ViewThatFits), no text truncation, cards still usable

    **Invite Flow (TEAM-01, TEAM-02):**
    9. Tap Care Team card > Tap "Invite Caregiver"
    10. Verify invite code shows in "CARE-XXXX-XXXX" format
    11. Tap "Copy" — verify code copies to clipboard
    12. Tap "Share" — verify share sheet appears

    **Care Team List (TEAM-03, TEAM-04):**
    13. View care team list (will be empty initially — verify ContentUnavailableView)

    **Permissions (TEAM-05, TEAM-06):**
    14. If a test member exists, tap into member detail
    15. Verify 4 permission toggles (Medications, Mood, Care Visits, Calendar)
    16. Toggle one off — verify undo toast appears briefly

    **Emergency Contacts (TEAM-09):**
    17. Navigate to Emergency Contacts
    18. Add a contact (name, phone, relationship)
    19. Verify it appears in the list

    20. Force-quit the simulator app and relaunch — verify data persists (SYNC-01)
  </how-to-verify>
  <verify>User confirms all visual and functional checks pass</verify>
  <done>User approves Phase 1 visual and functional verification</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Full test suite: zero failures
- CareTeamListView NavigationLink wired to MemberDetailView (not placeholder)
- Visual: senior home screen readable at AX text sizes
- Visual: invite code displays correctly
- Functional: data persists across app relaunch
</verification>

<success_criteria>
- All 10 test suites pass
- CareTeamListView navigates to MemberDetailView
- Senior home screen verified accessible at large Dynamic Type sizes
- Invite flow generates and shares codes
- Permission toggles work with undo toast
- Emergency contacts persist
- Data survives app force-quit and relaunch
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-05-SUMMARY.md`
</output>
