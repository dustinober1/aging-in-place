---
phase: 01-foundation
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - AgingInPlace/Features/SeniorHome/SeniorHomeView.swift
  - AgingInPlace/Features/SeniorHome/SummaryCardView.swift
  - AgingInPlace/Features/CaregiverHome/CaregiverHomeView.swift
  - AgingInPlace/Features/EmergencyContacts/EmergencyContactListView.swift
  - AgingInPlace/Features/EmergencyContacts/EmergencyContactFormView.swift
  - AgingInPlace/App/RootView.swift
  - AgingInPlaceTests/EmergencyContactTests.swift
  - AgingInPlaceTests/SeniorHomeTests.swift
  - AgingInPlaceTests/CaregiverHomeTests.swift
autonomous: true
requirements:
  - SENR-01
  - SENR-02
  - SENR-03
  - SENR-04
  - TEAM-08
  - TEAM-09

must_haves:
  truths:
    - "Senior home screen shows personalized greeting and large tappable cards for Medications, Mood, Care Team, and Calendar"
    - "All senior-facing touch targets are at minimum 44pt"
    - "Senior home screen uses iOS system colors that pass WCAG AAA contrast"
    - "Any primary action is reachable within 2 taps from the home screen"
    - "Caregiver home screen shows recent activity overview across care team"
    - "Emergency contacts are accessible to all care team members regardless of permissions"
  artifacts:
    - path: "AgingInPlace/Features/SeniorHome/SeniorHomeView.swift"
      provides: "Senior home with greeting and summary cards"
      contains: "SummaryCardView"
    - path: "AgingInPlace/Features/SeniorHome/SummaryCardView.swift"
      provides: "Reusable large card component with 44pt touch targets"
      contains: "minHeight: 44"
    - path: "AgingInPlace/Features/CaregiverHome/CaregiverHomeView.swift"
      provides: "Caregiver home with recent activity feed"
      contains: "@Query"
    - path: "AgingInPlace/Features/EmergencyContacts/EmergencyContactListView.swift"
      provides: "Emergency contact list accessible to all members"
      contains: "EmergencyContact"
  key_links:
    - from: "AgingInPlace/App/RootView.swift"
      to: "AgingInPlace/Features/SeniorHome/SeniorHomeView.swift"
      via: "Role-based navigation branching"
      pattern: "SeniorHomeView"
    - from: "AgingInPlace/App/RootView.swift"
      to: "AgingInPlace/Features/CaregiverHome/CaregiverHomeView.swift"
      via: "Role-based navigation branching"
      pattern: "CaregiverHomeView"
    - from: "AgingInPlace/Features/SeniorHome/SeniorHomeView.swift"
      to: "AgingInPlace/Features/SeniorHome/SummaryCardView.swift"
      via: "Card instances for each section"
      pattern: "SummaryCardView"
---

<objective>
Build the senior and caregiver home screens, emergency contact management, and role-based navigation — delivering the user-facing shell of the app with accessibility-first design.

Purpose: The senior home screen is the primary interface for the target user. It must be accessible from day one (SENR-01 through SENR-04). The caregiver home provides activity overview (TEAM-08). Emergency contacts bypass permissions (TEAM-09).
Output: Senior home with greeting + cards, caregiver home with activity feed, emergency contact CRUD, role-based RootView routing.
</objective>

<execution_context>
@/Users/dustinober/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dustinober/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — models needed for home screens -->

From AgingInPlace/Models/CareCircle.swift:
```swift
@Model final class CareCircle {
    var id: UUID
    var seniorName: String
    var members: [CareTeamMember]
    var pendingInvites: [InviteCode]
    var lastModified: Date
}
```

From AgingInPlace/Models/EmergencyContact.swift:
```swift
@Model final class EmergencyContact {
    var id: UUID
    var name: String
    var phone: String
    var relationship: String
    var medicalNotes: String?
    var lastModified: Date
}
```

From AgingInPlace/Models/CareRecord.swift:
```swift
@Model final class CareRecord {
    var id: UUID
    var category: PermissionCategory
    var encryptedPayload: Data
    var authorMemberID: UUID
    var createdAt: Date
    var lastModified: Date
}
```

From AgingInPlace/Design/Accessibility.swift:
```swift
static let minTouchTarget: CGFloat = 44
static let minCardHeight: CGFloat = 80
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Senior home screen with greeting and summary cards</name>
  <files>
    AgingInPlace/Features/SeniorHome/SeniorHomeView.swift
    AgingInPlace/Features/SeniorHome/SummaryCardView.swift
    AgingInPlace/App/RootView.swift
  </files>
  <action>
    **SummaryCardView.swift** — Reusable large tappable card component following the exact pattern from 01-RESEARCH.md Pattern 6:
    - Props: title (String), summary (String), systemImage (String), action (() -> Void)
    - Layout: HStack with SF Symbol icon (44x44 frame), VStack with title (.headline) and summary (.subheadline), chevron.right
    - Padding, .frame(minHeight: 80), .background(Color(uiColor: .secondarySystemBackground)), rounded corners 12pt
    - Button wrapper for tap action
    - .accessibilityLabel("\(title): \(summary)"), .accessibilityHint("Double tap to open")
    - Use `ViewThatFits` to switch from HStack to VStack at AX text sizes (per research pitfall 5)
    - All system colors (Color.primary, Color.secondary, Color.accentColor) — no custom palette
    - Minimum touch target enforced via .frame(minHeight: 44) on the button

    **SeniorHomeView.swift** — Senior's main screen per user decision:
    - Personalized greeting header: "Good morning/afternoon/evening, [Name]" using time-of-day logic
      - morning: 0-11, afternoon: 12-16, evening: 17-23
      - Name from CareCircle.seniorName via @Query
      - .font(.largeTitle).fontWeight(.bold)
    - ScrollView with single-column VStack, spacing 16
    - 4 SummaryCardView instances:
      1. "Medications" — pills.fill — summary placeholder "No medications yet" (real data comes in Phase 2)
      2. "Mood" — heart.fill — summary placeholder "Not recorded today"
      3. "Care Team" — person.3.fill — summary showing member count
      4. "Calendar" — calendar — summary placeholder "No upcoming appointments"
    - NavigationStack wrapping the entire view
    - Each card navigates to a detail view (placeholder NavigationLink destinations for Phase 2 features; Care Team navigates to CareTeamListView from Plan 02)
    - Navigation depth: home → detail = 1 tap. Meets SENR-03 (max 2 taps to primary action).
    - Emergency Contacts accessible via toolbar button or additional card

    **RootView.swift** — Update the placeholder from Plan 01:
    - `@AppStorage("userRole") var userRole: String?`
    - If userRole is nil: show RoleSelectionView (simple picker: "I am the Senior" / "I am a Caregiver")
    - If "senior": show SeniorHomeView in NavigationStack
    - If "caregiver": show CaregiverHomeView in NavigationStack
    - Tab bar or simple conditional — use TabView with tabs for Home, Care Team, Emergency, Settings

    Use system fonts (.largeTitle, .headline, .subheadline) and system colors only. No custom color assets.
  </action>
  <verify>
    <automated>xcodebuild build -scheme AgingInPlace -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -5</automated>
  </verify>
  <done>SeniorHomeView renders greeting + 4 summary cards. SummaryCardView uses 44pt touch targets, system colors, ViewThatFits for AX sizes. RootView routes based on role selection. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Caregiver home, emergency contacts, and unit tests</name>
  <files>
    AgingInPlace/Features/CaregiverHome/CaregiverHomeView.swift
    AgingInPlace/Features/EmergencyContacts/EmergencyContactListView.swift
    AgingInPlace/Features/EmergencyContacts/EmergencyContactFormView.swift
    AgingInPlaceTests/EmergencyContactTests.swift
    AgingInPlaceTests/SeniorHomeTests.swift
    AgingInPlaceTests/CaregiverHomeTests.swift
  </files>
  <action>
    **CaregiverHomeView.swift** — Caregiver's main screen (Claude's discretion on layout per CONTEXT.md):
    - Greeting header with caregiver's name
    - "Recent Activity" section — @Query on CareRecord sorted by lastModified descending, showing latest records across all categories the caregiver has permission for
    - Each activity row shows: category icon, timestamp, author name
    - Records in categories NOT granted to this caregiver are filtered out (per user decision: "Revoked categories hidden entirely from caregiver's view")
    - ContentUnavailableView for empty state ("No recent activity")
    - Quick action buttons for common tasks (placeholder destinations for Phase 2)
    - Uses system fonts and colors, 44pt touch targets

    **EmergencyContactListView.swift** — List of emergency contacts:
    - @Query to fetch all EmergencyContact objects
    - Each row: name, relationship, phone number
    - Tap to call (tel: URL scheme)
    - Swipe to delete
    - "Add Contact" button navigates to form
    - ContentUnavailableView for empty state
    - Per user decision: "Emergency contacts and medical ID always visible to all care team members regardless of permissions" — no permission filtering on this view

    **EmergencyContactFormView.swift** — Add/edit emergency contact:
    - TextFields for name, phone, relationship
    - Optional medicalNotes TextEditor
    - Save button creates/updates EmergencyContact, explicit save()
    - Form validation: name and phone required
    - 44pt touch targets on all inputs

    **EmergencyContactTests.swift** — Unit tests (in-memory ModelContainer):
    - Create EmergencyContact, save, fetch — persists correctly (TEAM-09)
    - EmergencyContact has no permission gating (accessible without category check)
    - Delete EmergencyContact removes from store

    **SeniorHomeTests.swift** — Unit test:
    - greetingForTimeOfDay returns correct string for morning/afternoon/evening hours (SENR-04)
    - Verify summary card data sources exist (categories match expected 4)

    **CaregiverHomeTests.swift** — Unit test (in-memory ModelContainer):
    - Query returns CareRecords sorted by lastModified descending (TEAM-08)
    - Records filtered by granted categories (revoked categories excluded)
  </action>
  <verify>
    <automated>xcodebuild test -scheme AgingInPlace -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:AgingInPlaceTests/EmergencyContactTests -only-testing:AgingInPlaceTests/SeniorHomeTests -only-testing:AgingInPlaceTests/CaregiverHomeTests 2>&1 | tail -20</automated>
  </verify>
  <done>CaregiverHomeView renders recent activity filtered by permissions. EmergencyContactListView shows contacts with add/delete. All EmergencyContactTests, SeniorHomeTests, CaregiverHomeTests pass.</done>
</task>

</tasks>

<verification>
- Senior home shows greeting + 4 cards with correct accessibility
- Caregiver home shows filtered recent activity
- Emergency contacts persist and are accessible without permission checks
- Role selection routes to correct home screen
- All unit tests pass
- All touch targets >= 44pt
</verification>

<success_criteria>
- Senior home screen renders personalized greeting and Medications, Mood, Care Team, Calendar cards
- All cards use system fonts, system colors, 44pt touch targets, ViewThatFits for AX sizes
- Primary actions reachable in 2 taps maximum from home (SENR-03)
- Caregiver home shows recent activity filtered by granted permissions
- Emergency contacts CRUD works, accessible to all members regardless of permissions
- All EmergencyContactTests, SeniorHomeTests, CaregiverHomeTests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
