import SwiftUI

struct CareCircleSetupView: View {
    let role: UserRole
    let displayName: String
    let iCloudRecordID: String
    var body: some View { Text("Care Circle Setup — placeholder") }
}
